- name: Remove packages
  tags: packages
  become: yes
  apt:
    name: |
      cheese,
      evolution,
      simple-scan,
      gnome-weather
    state: absent

# yakuake?
- name: install packages
  tags: packages
  become: yes
  apt: 
    name: |
      firefox,
      chromium,
      keepassxc,
      htop,
      conky,
      curl,
      wget,
      xclip,
      micro,
      zsh,
      terminator,
      rxvt-unicode-256color,
      java-1.8.0-openjdk,
      gimp
    state: present
