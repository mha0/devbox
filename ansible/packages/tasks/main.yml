- name: Remove packages
  tags: packages
  become: yes
  apt:
    name: "openjdk-11-jre,openjdk-11-jre-headless"
    state: absent
    autoremove: yes

# yakuake?
- name: install packages
  tags: packages
  become: yes
  apt: 
    name: "firefox,chromium-browser,keepassxc,htop,conky,curl,wget,xclip,zsh,rxvt-unicode-256color,guake,terminator,gimp,openjdk-8-jdk"
    state: present
